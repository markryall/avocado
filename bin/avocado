#!/usr/bin/env ruby

$:.unshift File.dirname(__FILE__)+'/../lib'

require 'avocado/config'
require 'avocado/auth'
require 'avocado/api'

config = Avocado::Config.new
auth = Avocado::Auth(config)
api = Avocado::API(auth)
puts api.couple
